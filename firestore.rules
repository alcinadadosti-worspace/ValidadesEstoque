rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Plano gratuito sem autenticação — leitura e escrita liberadas
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
